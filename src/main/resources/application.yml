mybatis-plus:
  mapper-locations: classpath:mappers/*.xml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/qcc_crawler?useSSL=true&useUnicode=true&characterEncoding=utf8
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
logging:
  level:
    root: info
    com.itsu.mapper: debug